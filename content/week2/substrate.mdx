# outbox 메커니즘

3번 블록 (메커니즘 1장)의 보강 자료. **outbox가 척추**라는 게 오늘 라이브의 hinge.

## 다이어그램 v0

```mermaid
flowchart LR
    subgraph In["📥 inbox/ · 원자료"]
        D1[PR · 코드]
        D2[노션 · 회고]
        D3[메모 · 노트]
    end

    subgraph Proc["⚙️ 처리"]
        P1[/morning 커맨드]
        P2[/digest 커맨드]
    end

    subgraph Out["📤 outbox/ · 매일 산출"]
        O1[메타인지 1줄]
        O2[다음 한 줄 액션]
        O3[근거 인용]
    end

    D1 --> P1
    D2 --> P1
    D3 --> P2
    P1 --> O1
    P1 --> O2
    P1 --> O3
    P2 --> O1

    classDef in fill:#dee9fb,stroke:#3d6ff2,color:#1f47b3
    classDef proc fill:#faf0ce,stroke:#b58b18,color:#7a5c10
    classDef out fill:#dcf0e3,stroke:#2f8a5d,color:#1f5b3e
    class D1,D2,D3 in
    class P1,P2 proc
    class O1,O2,O3 out
```

## 핵심 원칙 3가지

### 1. inbox는 정리하지 않는다

원자료를 그대로 던져넣습니다. 분류 X, 태깅 X, 폴더 깊이 X. **정리는 처리 단계에서 LLM이 한다.**

### 2. outbox에서 거꾸로 설계한다

"매일 아침 받고 싶은 1줄"이 outbox다. outbox가 정해지면 처리가 따라오고, 처리가 정해지면 inbox 구조가 따라온다.

<Callout type="tip">
대부분의 PKM이 망하는 이유는 **inbox부터 설계하기 때문**. 노트 폴더 구조 짜다가 3개월이 지나간다.
</Callout>

### 3. 처리는 LLM이 잘 읽을 수 있어야 한다

- 평문 마크다운
- 시간순 (파일명 prefix)
- 링크는 절대 경로 또는 명시적 ID
- 메타데이터는 frontmatter 최소

## 폴더 구조 v0

```
my-learning-agent/
├── inbox/
│   ├── 2026-05-20-pr-list.md         # 원자료 그대로
│   ├── 2026-05-20-notion-export/
│   └── 2026-05-19-meeting-notes.md
├── outbox/
│   └── 2026-05-21-morning.md         # 매일 아침 산출
├── processed/
│   └── archive-by-week/              # inbox에서 처리 후 이동 (선택)
├── .claude/
│   └── commands/
│       ├── morning.md                # /morning 커맨드
│       └── digest.md                 # /digest 커맨드
└── README.md
```

## 처리 커맨드 v0 — `/morning`

```markdown
# /morning

inbox/ 의 최근 7일 데이터를 읽고 outbox/{오늘 날짜}-morning.md 파일을 생성한다.

## 출력 형식

다음 3줄로만 작성한다. 길게 쓰지 않는다.

1. **오늘 나는 뭘 모르고 있나?** — 한 줄
2. **그래서 오늘 뭐 할까?** — 10분 안에 시작할 수 있는 행동 1줄
3. **근거 인용** — inbox/ 어느 파일의 어느 줄에서 나왔는지 명시

## 작성 원칙

- 추측이 아니라 inbox/ 데이터에서 찾은 증거 기반
- "잘 모르겠어요" 금지. 데이터가 부족하면 "더 모을 데이터" 1줄 제안
- 매일 같은 패턴 X. 어제 outbox/와 다른 각도면 그것도 한 줄 메모
```

## 라이브에서 같이 수정할 점

3번 블록 15분 동안 다음 중 본인 케이스에 맞는 걸 같이 손봅니다.

| 항목 | 디폴트 | 커스텀 예시 |
|---|---|---|
| outbox 시각 | 매일 아침 7시 | 출근 직전 8시, 점심 12시 |
| 데이터 소스 | PR · 노션 · 노트 | Slack DM, 캘린더, 책 메모 |
| 1줄 형태 | 메타인지 한 줄 + 액션 한 줄 | 질문 한 줄, 코드 한 줄 |
| 누적 주기 | 7일 회고 | 주말 회고, 월간 회고 없음 |

<Callout type="info">
v0는 **잘못된 디폴트가 아니라 출발점**. 본인이 한 번이라도 outbox를 받아본 뒤 수정하는 게 훨씬 빠릅니다.
</Callout>
